package com.osmar.boutiqueos.settings.twofactor;

/** Para que se pidio el codigo. Un codigo de un proposito no sirve para otro. */
public enum TwoFactorPurpose {
    LOGIN,
    PASSWORD_RESET,
    CHANGE_CREDENTIALS
}
